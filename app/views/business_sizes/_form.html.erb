<%= form_for(@business_size) do |f| %>
  <% if @business_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@business_size.errors.count, "error") %> prohibited this business_size from being saved:</h2>

      <ul>
      <% @business_size.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
